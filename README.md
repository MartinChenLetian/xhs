# MBTI server (stub)

This is a minimal Node.js backend to satisfy the frontend API.

## Run

```bash
cd ~/Desktop/MBTI_Projects/server
npm run start
```

Default port: 8787

## Endpoints

- GET `/api/health`
- POST `/api/hook` (returns disabled placeholder)
- POST `/api/report` (returns disabled placeholder)
